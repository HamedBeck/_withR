

naturalSelection <- function(population){

}

geneticAlgorithm <- function(nGeneration,...){

  generation <- lapply(seq.int(100), FUN = function(df){
    df %>% print(.)
      # configureTrip() %>%
      # calculateDistance(., df )
  })

}



lapply(seq.int(10), FUN =function(x) {assign(x, df %>%
                                               configureTrip() %>%
                                               calculateDistance(. , df))})

stopCluster(clust)
